# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage
test-results
playwright-report

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*debug.log*

# local env files
.env*
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
serviceAccountKey.json

.firebase
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

/src/e2e/.auth/auth.json

.pnpm-debug.log*
.pnpm-error.log*
.pnpm-store

# Configuración local de Claude Code: es de cada máquina, no del proyecto.
.claude/settings.json
.claude/settings.local.json
# Las skills viven en .agents/skills/, que es lo versionado y la única copia buena. Claude Code solo
# las registra si están además en .claude/skills/, así que ahí se copian — y esa copia se queda en la
# máquina: versionarla sería tener dos SKILL.md que se pueden separar sin que nadie se entere.
.claude/skills/
.vscode
# Transcripciones de video: audio y texto de trabajo (ver src/scripts/transcribeVideos.ts)
/transcripciones/
